# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @hamsoya-monorepo/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t hamsoya-monorepo-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system hamsoya-monorepo-auth-service && \
          adduser --system -G hamsoya-monorepo-auth-service hamsoya-monorepo-auth-service

COPY apps/auth-service/dist hamsoya-monorepo-auth-service/
COPY apps/auth-service/package.json hamsoya-monorepo-auth-service/
RUN chown -R hamsoya-monorepo-auth-service:hamsoya-monorepo-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix hamsoya-monorepo-auth-service --omit=dev -f install

CMD [ "node", "hamsoya-monorepo-auth-service" ]
